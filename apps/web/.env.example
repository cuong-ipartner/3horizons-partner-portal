# --- Local ---
# VITE_DATA_MODE=seed
# VITE_SITE_URL=http://localhost:5173

# --- Production (Cloudflare Pages → Environment variables) ---
# Use Production + Preview; Vite bakes these at BUILD time.
VITE_DATA_MODE=supabase
VITE_SUPABASE_URL=https://twrtfsykittmfrhkjxkn.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...   # Dashboard → Project Settings → API → anon public
# Never put service_role in Vite / Cloudflare Pages env
VITE_SITE_URL=https://partners.3horizons.vn

# --- Server secrets only (never VITE_*, never commit real values) ---

# Nexus (Grok) — local Vite middleware + CF Pages Function /api/nexus
# XAI_API_KEY=xai-...

# LinkedIn real profile import — local /api/social-enrich + CF Function
# Get key: https://nubela.co/proxycurl/  (Person Profile API)
# Local: set below, restart `npm run dev`
# Production:
#   npx wrangler pages secret put PROXYCURL_API_KEY --project-name=3horizons-partner-portal
# PROXYCURL_API_KEY=
